import { SpatialLedger } from '@/components/sde/SpatialLedger';

export default function SDEPage() {
    return (
        <div className="w-full h-screen bg-canvas text-white">
            <SpatialLedger />
        </div>
    );
}
